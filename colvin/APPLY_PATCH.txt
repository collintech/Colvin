Colvin v0.13 -> v0.14 Gate 6B

1. Commit/back up the verified v0.13 Vincario checkpoint.
2. Extract this archive over the existing Colvin repository root.
3. Keep your local .env, package-lock.json, and Go go.sum files.
4. Add the Gate 6B HISTORY_PROVIDER_* values from .env.example to your local .env.
5. Keep HISTORY_PROVIDER_MODE=local until Vincario credentials and stolen-check entitlement are available.
6. Run:
   npm run format
   npm run quality
   npm run db:migrate
   npm run db:migrate
   npm run test:integration

Migration 007 is additive and should apply once, then skip on the second migration run.
