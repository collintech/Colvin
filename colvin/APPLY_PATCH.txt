Colvin v0.10 -> v0.10.1 integration teardown fix

Apply this patch over the root of your existing Colvin repository.

Changed:
- apps/api-gateway/test-integration/auth.integration.test.js

Purpose:
- close the auth integration verification pool
- close the API Gateway PostgreSQL pool
- close the API Gateway Redis client

This prevents the Node integration-test process from hanging after auth tests complete.

After applying:
  npm run format
  npm run quality
  npm run test:integration
